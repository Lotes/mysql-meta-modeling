/**
 * Define a grammar called Hello
 */
grammar Meta;

translationUnit  : 'hello' ID ;         // match keyword hello followed by an identifier

ID : [a-zA-Z_][a-zA-Z0-9_]*;             // match lower-case identifiers

WS : [ \t\r\n]+ -> skip ; // skip spaces, tabs, newlines

